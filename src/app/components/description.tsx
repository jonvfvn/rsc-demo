"use client";

export default function Description() {
    return (
        <div className="border-2 border-white/10 rounded-md p-4">
            <p>
                The code creates a counter React component with a button that increments a displayed number each time it&apos;s clicked. There is also an alternate version that can show custom text inside the button provided by the parent component.
            </p>
        </div>
    );
}
